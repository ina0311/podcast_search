# Skill: resume

## 概要

レート制限やセッション中断後に前回の作業を再開する。

## 手順

1. `*-progress.md` または `*-plan.md` ファイルを検索して読み込む
   ```bash
   find . -name "*-progress.md" -o -name "*-plan.md" | head -10
   ```
2. 完了済みの項目と残作業をサマリとして出力する
3. ユーザーに確認してから続行する
4. 完了後に進捗ファイルを更新（または削除）する
