# チーム構成

team-lead(sonnet) / researcher(haiku, 読取専用) / implementer-a(sonnet, apps/) / implementer-b(sonnet, packages/ infra/) / reviewer(opus)

ワークフロー: TaskCreate → TaskUpdate(owner) → in_progress → 実装 → SendMessage → completed
