# =============================================================================
# 共通ベースイメージ for Podcast Search
# pnpm + Node.js 22 Alpine
# =============================================================================

FROM node:22-alpine AS base
ENV PNPM_HOME="/pnpm" PATH="${PNPM_HOME}:${PATH}"
WORKDIR /app
RUN apk add --no-cache openssl curl && corepack enable


