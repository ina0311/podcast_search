{
  "install": "corepack enable && corepack prepare pnpm@10.14.0 --activate && pnpm install --frozen-lockfile",
  "terminals": [
    {
      "name": "API dev",
      "command": "pnpm run dev:api"
    },
    {
      "name": "Admin UI",
      "command": "pnpm run dev:admin-ui"
    }
  ]
}