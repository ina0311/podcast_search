node_modules
dist
build
*.tsbuildinfo
.DS_Store
# dotenvx - encrypted .env files are safe to commit
# Private keys (NEVER commit)
.env.keys

# Local unencrypted env (for local overrides)
.env.local
.env.*.local
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
coverage
.pnpm-store
pnpm-store

# Turborepo
.turbo

# Claude Code local settings (machine-specific)
.claude/settings.local.json
CLAUDE.local.md